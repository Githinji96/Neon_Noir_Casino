# Implementation Plan: Neon Noir Casino

## Overview

Implement a cyberpunk-themed iGaming SPA in React + TypeScript. The build proceeds in layers: project scaffold → config/data → game logic → state → lobby UI → slot machine UI → animations/polish.

## Tasks

- [x] 1. Scaffold project and configure tooling
  - Initialize Vite + React + TypeScript project
  - Install dependencies: tailwindcss, framer-motion, zustand, react-router-dom, vitest, @testing-library/react, fast-check
  - Configure TailwindCSS with custom theme: dark purple/black background, neon yellow (#FFD700), electric purple, cyan accent colors, futuristic font family
  - Set up `src/styles/index.css` with Tailwind directives and CSS custom properties
  - Configure Vitest in `vite.config.ts`
  - _Requirements: 1.1, 1.2, 1.4, 19.1, 19.2_

- [x] 2. Implement game configuration modules
  - [x] 2.1 Create `src/config/symbols.ts` with all 11 symbol definitions (id, label, emoji, weight, multiplier, isPremium) per the weight table in the design
    - _Requirements: 11.1, 19.6, 19.7_

  - [x] 2.2 Create `src/config/mockData.ts` exporting at least 6 `GameListing` entries and 3 `JackpotData` entries
    - _Requirements: 4.4, 5.5, 6.3, 19.6_

  - [x] 2.3 Create `src/config/betLadder.ts` exporting `BET_LADDER = [0.20, 0.50, 1.00, 2.00, 5.00, 10.00, 20.00, 50.00, 100.00]`
    - _Requirements: 10.2, 10.3, 19.7_

- [x] 3. Implement core game logic — RNG
  - [x] 3.1 Implement `src/logic/rng.ts` — `generateSpin(): SpinGrid` using weighted random selection per column, independently for each of 5 columns × 3 rows
    - _Requirements: 11.1, 11.2, 11.3, 11.4_

  - [ ]* 3.2 Write property test for spin grid validity (Property 1)
    - **Property 1: Spin grid is always complete and valid**
    - **Validates: Requirements 11.4**
    - Tag: `Feature: neon-noir-casino, Property 1`
    - Use `fc.constant(null)` → call `generateSpin()`, assert 5×3 shape and all cells are valid SymbolIds

  - [ ]* 3.3 Write property test for reel independence (Property 2)
    - **Property 2: Reel independence**
    - **Validates: Requirements 11.2**
    - Tag: `Feature: neon-noir-casino, Property 2`
    - Assert each column is generated by an independent draw (no column output derived from another)

  - [ ]* 3.4 Write property test for weighted symbol distribution (Property 3)
    - **Property 3: Weighted symbol distribution**
    - **Validates: Requirements 11.1**
    - Tag: `Feature: neon-noir-casino, Property 3`
    - Generate ≥1000 spins, assert observed symbol frequencies approximate configured weights within tolerance

- [x] 4. Implement core game logic — Paylines
  - [x] 4.1 Implement `src/logic/paylines.ts` — define `PAYLINES` (5 paylines: 3 rows + 2 diagonals) and `evaluatePaylines(grid: SpinGrid): WinResult[]`
    - Handle Wild substitution: treat Wild as any standard symbol to complete/extend matches
    - Detect Scatter trigger: return a scatter flag when ≥3 Scatter symbols appear anywhere in the grid
    - _Requirements: 12.1, 12.2, 12.3, 12.7_

  - [ ]* 4.2 Write property test for Wild substitution (Property 5)
    - **Property 5: Wild substitution in payline evaluation**
    - **Validates: Requirements 12.3**
    - Tag: `Feature: neon-noir-casino, Property 5`
    - Use `fc.array(symbolId)` with injected wilds; assert win detected when wild completes a 3+ match

  - [ ]* 4.3 Write property test for Scatter trigger threshold (Property 11)
    - **Property 11: Scatter trigger threshold**
    - **Validates: Requirements 12.7**
    - Tag: `Feature: neon-noir-casino, Property 11`
    - Assert grids with ≥3 scatters trigger free spins; grids with <3 do not

  - [ ]* 4.4 Write property test for win detection completeness (Property 12)
    - **Property 12: Win detection completeness**
    - **Validates: Requirements 12.1, 12.2**
    - Tag: `Feature: neon-noir-casino, Property 12`
    - Use `fc.array` of symbol grids; assert every qualifying payline is included and no false positives

- [x] 5. Implement core game logic — Payout
  - [x] 5.1 Implement `src/logic/payout.ts` — `calculatePayout(winResults: WinResult[], bet: number): number` using formula `bet × symbol.multiplier × matchCount`
    - _Requirements: 12.4_

  - [ ]* 5.2 Write property test for payout calculation correctness (Property 4)
    - **Property 4: Payout calculation correctness**
    - **Validates: Requirements 12.4**
    - Tag: `Feature: neon-noir-casino, Property 4`
    - Use `fc.record({ bet, multiplier, matchCount })` and assert payout equals `bet × multiplier × matchCount`

- [x] 6. Checkpoint — Ensure all logic tests pass
  - Ensure all tests pass, ask the user if questions arise.

- [x] 7. Implement Zustand store
  - [x] 7.1 Create `src/store/gameStore.ts` implementing the full `GameState` interface from the design
    - Initialize balance to $1,000.00, bet to $0.20, reels to a default grid, freeSpinsRemaining to 0
    - Implement `spin()`: deduct bet (if not free spin), call `generateSpin()`, call `evaluatePaylines()`, call `calculatePayout()`, update balance/lastWin/freeSpins
    - Implement `setBet(direction)` clamped to BET_LADDER boundaries
    - Implement toggle actions: `toggleAutoplay`, `toggleTurboMode`, `toggleSound`
    - _Requirements: 9.2, 9.5, 9.6, 9.7, 10.1–10.6, 12.5, 12.6, 13.1–13.5, 14.1–14.5, 19.3_

  - [ ]* 7.2 Write property test for balance deduction on spin (Property 6)
    - **Property 6: Balance deduction on spin**
    - **Validates: Requirements 14.2**
    - Tag: `Feature: neon-noir-casino, Property 6`
    - Use `fc.float({ min: 0.20, max: 1000 })` for balance/bet pairs; assert balance after spin initiation equals balance before minus bet

  - [ ]* 7.3 Write property test for balance addition on win (Property 7)
    - **Property 7: Balance addition on win**
    - **Validates: Requirements 12.6, 14.3**
    - Tag: `Feature: neon-noir-casino, Property 7`
    - Use `fc.record({ balance, winResults })`; assert final balance equals pre-result balance plus total payout

  - [ ]* 7.4 Write property test for free spins no balance deduction (Property 8)
    - **Property 8: Free spins do not deduct balance**
    - **Validates: Requirements 13.2**
    - Tag: `Feature: neon-noir-casino, Property 8`
    - Use `fc.integer({ min: 1, max: 10 })` for free spin count; assert balance unchanged at spin initiation

  - [ ]* 7.5 Write property test for free spins count decrement (Property 9)
    - **Property 9: Free spins count decrements correctly**
    - **Validates: Requirements 13.1, 13.4**
    - Tag: `Feature: neon-noir-casino, Property 9`
    - Assert count decreases by exactly 1 per free spin and reaches 0 after 10 spins

  - [ ]* 7.6 Write property test for bet ladder boundary enforcement (Property 10)
    - **Property 10: Bet ladder boundary enforcement**
    - **Validates: Requirements 10.4, 10.5**
    - Tag: `Feature: neon-noir-casino, Property 10`
    - Use `fc.constantFrom(...BET_LADDER)`; assert min bet unchanged on minus, max bet unchanged on plus

- [x] 8. Checkpoint — Ensure all store tests pass
  - Ensure all tests pass, ask the user if questions arise.

- [x] 9. Implement shared UI components
  - [x] 9.1 Implement `src/components/Navbar.tsx`
    - Display logo "NEON NOIR CASINO", nav links (Slots, Live Tables, Jackpots, VIP), balance formatted as currency, Deposit button, avatar/notifications/settings icons
    - Active link highlighted with neon yellow; hamburger menu below 768px
    - Read balance from Zustand store
    - _Requirements: 2.1–2.7_

  - [ ]* 9.2 Write unit tests for Navbar
    - Test balance formatting (e.g. `25400` → `$25,400.00`)
    - Test active link styling
    - _Requirements: 2.3, 2.6_

  - [x] 9.3 Implement `src/components/GameCard.tsx`
    - Display thumbnail, title, badge (HOT/NEW); hover: neon border glow + scale(1.05) via Framer Motion (≤300ms)
    - _Requirements: 4.2, 4.3, 1.5_

  - [x] 9.4 Implement `src/components/JackpotCard.tsx`
    - Display jackpot name, animated prize amount, tags, "Spin Now" button
    - _Requirements: 5.2, 5.3_

  - [x] 9.5 Implement `src/components/ParticleBackground.tsx`
    - Render canvas/div-based low-opacity neon particle drift animation
    - Disable when `prefers-reduced-motion` is active
    - _Requirements: 18.1–18.4_

  - [x] 9.6 Implement `src/components/PaytableModal.tsx`
    - Modal overlay with premium symbol rules (Wild Noir, Cyber Volt), standard symbol multipliers table, RTP 96.5% and Volatility High footer, "Back to Game" close button
    - _Requirements: 15.1–15.6_

  - [ ]* 9.7 Write property test for paytable symbol multipliers (Property 13)
    - **Property 13: Paytable displays all symbol multipliers**
    - **Validates: Requirements 15.4**
    - Tag: `Feature: neon-noir-casino, Property 13`
    - Use `fc.constantFrom(...standardSymbols)`; assert each symbol's multiplier value is rendered in the modal

  - [ ]* 9.8 Write unit tests for PaytableModal
    - Test open/close behavior
    - _Requirements: 15.2, 15.6_

  - [x] 9.9 Implement `src/components/BottomNav.tsx`
    - Tabs: Home, Promos, Spin, History, Support; active tab highlighted neon yellow; hidden at ≥768px
    - _Requirements: 7.1–7.4_

- [x] 10. Implement Casino Lobby page
  - [x] 10.1 Implement `src/pages/CasinoLobby.tsx` as the root layout composing all lobby sections, Navbar, BottomNav, and ParticleBackground
    - _Requirements: 3.1, 19.5_

  - [x] 10.2 Implement `src/pages/HeroSection.tsx`
    - Headline "UNLEASH THE NEON WIN", animated jackpot counter (starts at $3,429,102.55, increments $0.01–$2.00 every 100ms), "Play Now" and "Details" CTAs, blurred background image with dark overlay
    - "Play Now" navigates to `/slot`
    - _Requirements: 3.1–3.7_

  - [ ]* 10.3 Write unit tests for HeroSection jackpot counter
    - Test counter increments within $0.01–$2.00 range per tick
    - _Requirements: 3.4_

  - [x] 10.4 Implement `src/pages/NewArrivalsSection.tsx`
    - Responsive grid of ≥6 GameCard components from mockData; clicking any card navigates to `/slot`; shimmer placeholders during loading state
    - _Requirements: 4.1, 4.4, 4.5, 17.1_

  - [x] 10.5 Implement `src/pages/ProgressiveJackpotsSection.tsx`
    - Horizontally scrollable list of ≥3 JackpotCard components from mockData; "Spin Now" navigates to `/slot`
    - _Requirements: 5.1–5.5_

  - [x] 10.6 Implement `src/pages/PopularChoicesSection.tsx`
    - Grid of ≥4 cards showing game avatar, RTP %, and Volatility level from mockData; clicking navigates to `/slot`
    - _Requirements: 6.1–6.4_

- [x] 11. Checkpoint — Ensure lobby renders correctly with all sections
  - Ensure all tests pass, ask the user if questions arise.

- [x] 12. Implement Slot Machine page
  - [x] 12.1 Implement `src/pages/SlotMachine/SymbolCell.tsx`
    - Render symbol emoji/icon on dark background with neon styling; apply pulse animation when `isWinning` is true (2s duration)
    - _Requirements: 8.2, 8.3, 8.5, 16.2_

  - [x] 12.2 Implement `src/pages/SlotMachine/ReelColumn.tsx`
    - Render 3 SymbolCells; animate top-to-bottom easing scroll on spin using Framer Motion; stop after `stopDelay` ms; highlight winning rows with neon border
    - Turbo mode reduces animation duration by 70%
    - _Requirements: 8.4, 9.3, 9.7_

  - [x] 12.3 Implement `src/pages/SlotMachine/ReelGrid.tsx`
    - Compose 5 ReelColumn components; stagger stop delay by `colIndex * 150ms`
    - _Requirements: 8.1, 9.3_

  - [x] 12.4 Implement `src/pages/SlotMachine/BettingControls.tsx`
    - Minus/Plus buttons stepping through BET_LADDER; numeric bet display; disable minus at min, plus at max; show "Insufficient Balance" when balance < bet
    - _Requirements: 10.1–10.6_

  - [ ]* 12.5 Write unit tests for BettingControls
    - Test min/max clamping, insufficient balance indicator
    - _Requirements: 10.4, 10.5, 10.6_

  - [x] 12.6 Implement `src/pages/SlotMachine/SpinControls.tsx`
    - SPIN button (neon yellow, glowing shadow); disabled while spinning or balance < bet or balance = $0; Autoplay toggle; Turbo Mode toggle; sound toggle
    - _Requirements: 9.1, 9.4, 9.5, 9.7, 14.5, 16.5_

  - [x] 12.7 Implement `src/pages/SlotMachine/WinDisplay.tsx`
    - Show "Last Win" amount; animated win announcement overlay on win; neon payline highlight overlay; winning payline neon line drawn over ReelGrid
    - _Requirements: 12.5, 16.1, 16.3_

  - [x] 12.8 Implement `src/pages/SlotMachine/FreeSpinsBanner.tsx`
    - Prominently display remaining free spin count while active; show summary modal (total winnings) when free spins end
    - _Requirements: 13.3, 13.5_

  - [x] 12.9 Implement `src/pages/SlotMachine.tsx` as the root slot page
    - Compose Navbar, ReelGrid, SpinControls, BettingControls, WinDisplay, FreeSpinsBanner, PaytableModal
    - Connect all components to Zustand store; show loading animation on init; handle autoplay loop (re-trigger spin after animation completes, stop when balance = $0)
    - _Requirements: 9.5, 9.6, 14.1, 17.2, 19.5_

- [x] 13. Implement routing and app entry point
  - Create `src/App.tsx` with React Router v6: route `/` → CasinoLobby, route `/slot` → SlotMachine
  - _Requirements: 19.1_

- [x] 14. Apply design system polish and animations
  - Apply Glassmorphism styling (backdrop-blur, translucent panels, soft borders) to all card and modal components
  - Add hover glow animations (≤300ms) to all interactive elements via Tailwind/Framer Motion
  - Add pulsing animations to jackpot counters and promotional banners
  - Verify responsive layout from 320px to 1920px across all components
  - Add shimmer gradient sweep animation for loading states in lobby
  - _Requirements: 1.3, 1.5, 1.6, 1.7, 17.1, 17.3_

- [x] 15. Final checkpoint — Ensure all tests pass
  - Ensure all tests pass, ask the user if questions arise.

## Notes

- Tasks marked with `*` are optional and can be skipped for a faster MVP
- Each task references specific requirements for traceability
- Property tests use fast-check with a minimum of 100 iterations (`numRuns: 100`)
- Unit tests use Vitest + React Testing Library
- All game logic functions (`generateSpin`, `evaluatePaylines`, `calculatePayout`) are pure — test them without React
- Sound effects (Requirement 16.4) are wired via the `soundEnabled` store flag; actual audio assets can be placeholder no-ops initially
